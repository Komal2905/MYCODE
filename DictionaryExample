#import <Foundation/Foundation.h>

int main(int argc, const char* argv[]){
    NSAutoreleasePool *pool = [[NSAutoreleasePool alloc] init];
    NSDictionary *price=@{@"mango" :@50, @"Grapes" :@30};
NSLog(@"%@",price);
    [pool drain];
    return 0;
}
